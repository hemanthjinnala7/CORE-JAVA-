DRAWBACK OF AN ARRAY 
-> array is an object created by using new keyword takes heap memory
-> it will occupy some space and memory locations it takes is continous 
-> once you assign a size to an array , you cant modify the size anymore
 -> we cant store different types of data in the array

public class Main
{
    public static void main(String a[])
    {

        int arr[][] = new int [3][4];
        for(int i=0;i<3;++i)
        {
            for(int j=0;j<4;++j)
            {
                arr[i][j]= (int)(Math.random() * 10);
            }

        }
        for(int i=0;i<3;++i)
        {
            for(int j=0;j<4;++j)
            {
                System.out.print(arr[i][j]);
                System.out.print(" ");
            }
            System.out.println();
        }

    }
}
